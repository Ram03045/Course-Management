<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= course.courseName %></title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="container">
        <h1><%= course.courseName %></h1>
        <img src="<%= course.image %>" alt="<%= course.courseName %>" class="course-img">
        <p><strong>Price:</strong> ₹<%= course.price %></p>
        <p><strong>Duration:</strong> <%= course.duration %></p>
        <p><strong>Start Date:</strong> <%= course.courseStartDate.toDateString() %></p>
        <a href="/courses/<%= course._id %>/edit" class="edit-btn">Edit</a>
        <form action="/courses/<%= course._id %>?_method=DELETE" method="POST">
            <button type="submit" class="delete-btn">Delete</button>
        </form>
        <a href="/courses" class="back-btn">Back to Courses</a>
    </div>
</body>
</html>
